using System.Collections;
using System.Collections.Generic;
using Photon.Pun;
using UnityEngine;

public class ActiveRoomList : MonoBehaviour
{
    public void UpdateRoomList(string roomName) {
        // Check if specified room exists
        // bool roomExist = PhotonNetwork.GetCustomRoomList(new Photon.Realtime.TypedLobby(u"SELECT * FROM rooms WHERE Name eq test", );
        // (room => Regex. room.name) > 0;
        // if (roomExist)
        // {
        //     _errorState = "Specified room alredy exists";
        // }
    }


}
